# ITEC User Manual

Static HTML user guide for all ITEC dashboards.

## Live

Deploy `index.html` and the `screenshots/` folder to any static host (e.g. Vercel).

## Contents

- `index.html` — complete user manual
- `screenshots/` — dashboard screenshots by role
- `build-manual.mjs` — script used to generate the manual
